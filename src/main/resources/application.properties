# Server Configuration
server.port=${PORT:5000}
# Removed context path to avoid URL conflicts
# server.servlet.context-path=/KalviTrack

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://kalvitrack-db.cp0ewm0wmq4f.eu-north-1.rds.amazonaws.com:3306/kalvitrack
spring.datasource.username=kalvitrack_user
spring.datasource.password=Karthika_1312


spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Connection pool settings
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.idle-timeout=300000

# Hibernate / JPA
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.time_zone=UTC+05:30

# JWT Configuration - Secure 256-bit secret
jwt.secret=mySecretKeyForKalviTrackAppThatIsAtLeast32CharactersLongForHS256Algorithm
jwt.expiration=86400000
# CORS configuration - Updated to include port 5174
# Update this line in application.properties
# CORS configuration
# CORS configuration
cors.allowed-origins=https://kalvitrack.vercel.app,https://www.kalvi-track.co.in,https://d1clpzx8i9nb2e.cloudfront.net
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
spring.servlet.multipart.enabled=true
logging.level.org.hibernate.SQL=debug
logging.level.org.springframework.orm.jpa=debug

# Session Configuration
server.servlet.session.tracking-modes=cookie
server.servlet.session.timeout=30m

# Upload directory configuration
app.upload.dir=./uploads

# Logging Configuration
logging.level.com.kalvitrack=INFO
logging.level.org.springframework.web=DEBUG
logging.level.org.springframework.security=DEBUG

# Error handling
# Email Configuration for Password Reset
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=lakshmikarthika07@gmail.com
spring.mail.password=fmsw qnfc zvyr tdah
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Password Reset Configuration
app.password.reset.token.expiry.hours=1
app.password.reset.max.attempts=3
app.password.reset.lockout.minutes=30

# Frontend URL for reset links
app.frontend.url=https://kalvitrack.vercel.app
app.frontend.reset-url=${app.frontend.url}/reset-password
